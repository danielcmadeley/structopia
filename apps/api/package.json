{
  "name": "api",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "python -m venv venv && source venv/bin/activate && pip install -r requirements.txt && python api/main.py",
    "start": "python api/main.py"
  }
}
