{
  "name": "api",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "python -m venv venv && . venv/bin/activate && pip install -r requirements.txt && python main.py",
    "start": "python main.py"
  }
}
